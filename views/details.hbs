<main>
  <h1>{{currentCube.name}}</h1>
  <img class="cube"
    src="{{currentCube.imageUrl}}">
  <div class="details">
    <p><span>Description:</span> {{currentCube.description}}</p>
    <p><span>Difficulty level:</span>{{currentCube.difficulty}}</p>
    <a class="btn" href="/">Back</a>
  </div>
</main>